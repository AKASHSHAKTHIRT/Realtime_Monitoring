# TEMPORARY — for local testing only.
# Once Ingress (Phase 3) is set up, this will be removed/replaced.
apiVersion: v1
kind: Service
metadata:
  name: flask-nodeport
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080   # accessible at <minikube-ip>:30080
